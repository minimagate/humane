<% content_for :title, "New skill — Humane" %>

<div class="flex min-h-dvh bg-canvas">
  <%= render "agents/sidebar", display: "hidden sm:flex" %>
  <section class="min-w-0 flex-1">
    <header class="flex h-14 items-center px-5 sm:px-6"><h1 class="text-sm font-semibold tracking-[-0.015em] text-ink">New skill</h1></header>
    <div class="mx-auto max-w-2xl px-5 py-5 sm:px-6 sm:py-6"><%= render "form", skill: @skill %></div>
  </section>
</div>
