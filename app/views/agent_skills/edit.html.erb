<% content_for :title, "#{@skill.name} — Humane" %>

<div class="flex min-h-dvh bg-[#0A0A0A]">
  <%= render "agents/sidebar", display: "hidden sm:flex" %>
  <section class="min-w-0 flex-1 bg-[#0A0A0A]">
    <header class="flex h-16 items-center border-b border-white/[0.08] px-6 sm:px-8"><h1 class="text-sm text-zinc-200"><%= @skill.name %></h1></header>
    <div class="px-6 py-10 sm:px-10"><%= render "form", skill: @skill %></div>
  </section>
</div>
