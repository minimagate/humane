interface:
  display_name: "Frontend"
  short_description: "Build server-rendered Rails interfaces"
  default_prompt: "Use $frontend to implement this accessible Rails interface with ERB, Turbo, Stimulus, and the existing frontend stack."
